.terraform/
*.tfstate
*.tfstate.*
crash.log

__pycache__/
.pytest_cache
.ruff_cache
.coverage
coverage.xml
htmlcov/

# sonar-scanner local run artifacts
.scannerwork/

# Caller-supplied terraform staged into the custom module at deploy time.
# Only the platform's _-prefixed files belong in the repo. _versions.tf was
# removed (fold into the root module's required_providers to avoid a
# duplicate required_providers block once _providers.g.tf is generated).
terraform/azure/custom/*
!terraform/azure/custom/_context.tf
