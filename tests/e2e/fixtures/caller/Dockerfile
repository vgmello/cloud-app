# Minimal app image for the e2e caller fixture. Never built for real: the fake
# cloud shims `docker`, which only checks the Dockerfile and context exist.
FROM nginxinc/nginx-unprivileged:stable-alpine
EXPOSE 8080
